LIST firlistBUILD_NUM.txt

##parallel FIR
BUILD
  SWAP ORDER 3
  SWAP COEFF_BITS 12
  SWAP DIN_BITS 8
  SWAP MAC_NUM 4
ENDBUILD

##Nserial FIR
BUILD
  SWAP ORDER 3
  SWAP COEFF_BITS 16
  SWAP DIN_BITS 24
  SWAP MAC_NUM 2
ENDBUILD

##serial FIR
BUILD
  SWAP ORDER 7
  SWAP COEFF_BITS 8
  SWAP DIN_BITS 32
  SWAP MAC_NUM 1
ENDBUILD
